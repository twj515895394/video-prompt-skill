---
name: video-prompt-skill
description: 当用户需要根据文字或参考图生成文生视频、图生视频、多镜头时间轴或镜头级视频提示词时，应使用此技能。
---

# Video Prompt Skill

## 使用目标

把用户的叙事目标转成可执行的视频 Prompt、镜头级时间轴和连续性限制，让结果既有气氛，也真的有运动、节奏和收尾。

## 何时使用

在以下场景触发：

- 文生视频
- 图生视频
- 单片段视频
- 多片段视频
- 镜头级时间轴设计

不处理以下场景：

- 角色立绘生成
- 场景设定图生成
- 静态封面海报生成
- 视频链接下载、抽帧、解析

## 核心承诺

- 默认产出能直接投喂模型的内容，而不是讲一堆概念。
- 先解决“谁在动、怎么动、镜头怎么跟”，再谈风格气质。
- 交互模式遵循 `grill-me`：每次只问一个问题，不设固定轮数上限。
- 不把“电影感、大片感、故事感”直接塞进 Prompt，必须拆成时间顺序、主体运动、镜头运动、光影色调和连续性约束。
- 不暴露项目维护路径、真源路径或同步语义。

## 先做判断

### 1. 判断任务来源

- `图生视频`
- `文生视频`

有图时优先走图生视频逻辑，不要偷懒套文生视频模板。

### 2. 判断输出结构

- `单片段`
- `多片段`

默认把“一个动作 / 一个情绪变化 / 一个镜头主任务”视为单片段，其余按多片段处理。

### 3. 判断输入形态

- 纯文字
- 单张参考图
- 多张参考图

多图输入时先判：

- 哪张是主图
- 其余分别承担什么职责：角色、场景、首帧、尾帧、分镜、光影、服装、动作

### 4. 判断模式

- 明显直接要结果：走 `快速模式`
- 明显要共创、找方向、讨论镜头策略：走 `交互模式`
- 意图模糊但没要求共创：仍默认 `快速模式`

## 运行流程

1. 先读取 `references/index.md`。
2. 只加载：
   - `1` 份模式 reference
   - `1` 份输入 reference
   - `1` 份任务 reference
   - `timeline/timeline-assembly.md`
   - `continuity/continuity-guardrails.md`
   - `0-2` 份高价值控制 reference
   - `0-1` 份输出结构 reference
   - `0-2` 份附录
3. 先锁主体运动和镜头主任务，再补风格和光影。
4. 对外默认展示镜头级时间轴，不展示内部全部推理。
5. 输出前自查：
   - 时间顺序是否清楚
   - 主体运动与镜头运动是否分开
   - 连续性限制是否足够
   - 是否有自然收尾

### 高频控制页触发

当用户明确追求以下目标时，优先补载高价值控制页，而不是只靠时间轴骨架自行发散：

- “像真实镜头”“别像动起来的 AI 图”
- “镜头别乱飘”“别太炫”“别像游戏运镜”
- “要自然收尾”“要有生活化落点”
- “别变脸”“跨帧人物要稳”“动作别跳”

高频控制页包括：

- `references/style-control/anti-ai-video-realism.md`
- `references/style-control/camera-failure-patterns-negative.md`
- `references/continuity/human-motion-consistency.md`
- `references/timeline/lifestyle-beat-and-landing.md`

## 快速模式

快速模式是“不打扰用户但保证可跑”，不是把所有空白都靠风格词糊过去。

### 允许主动补全

- 单片段默认时长
- 镜头切分密度
- 镜头运动幅度
- 光影与色调支撑
- 连续性与失败限制

### 不允许擅自锁死

- 没有暗示的新剧情线
- 新角色
- 大世界观设定
- 强情绪反转
- 用户没说过的复杂段落结构

### 即使在快速模式也要追问

- 参考图类型无法判断
- 主图和辅助图相互冲突
- 用户要求既“完全复刻”又“彻底重做运动策略”
- 多片段任务缺少核心节拍，导致时长和内容无法对应

## 交互模式

交互模式严格遵循 `grill-me`。

### 硬规则

- 每次只问一个问题
- 不设固定轮数
- 能从现有内容推断的，不追问用户
- 只追问最影响视频结果的那个变量
- 允许指出矛盾，例如“要极稳长镜头，但又想每秒都爆点”

### 起手方式

如果用户方向还没锁定，先给 `2-3` 个方向假设，例如：

- 更偏情绪电影短片
- 更偏动作推进
- 更偏角色关系段落

如果方向已经明确，直接追当前最大缺口，不必机械给三选项。

### 追问优先级

- 图生视频：图像类型 > 必保锚点 > 主体运动 > 镜头运动 > 节拍
- 文生视频：开场画面 > 主体运动 > 镜头任务 > 节奏分段 > 收尾状态
- 单片段：唯一主任务 > 镜头力度 > 收尾
- 多片段：段落目标 > 承接关系 > 跨段稳定项

### 收口条件

满足以下任一条件即可停止追问：

- 用户说“够了，出结果”
- 再问只会做微调，不再改变主方向
- 开场、推进、收尾和连续性都已可稳产出

## 时间轴规则

- 内部允许按片段单元推理
- 对外默认只展示镜头级时间轴
- 时间轴统一使用绝对时间
- 每一段都必须写：
  - 主体状态
  - 镜头状态
  - 节拍变化

## 输出约束

- 使用 `中文主描述 + 英文关键词增强`
- 必带连续性与失败限制项
- 默认给 `主版本 + 备选版本`
- 快速模式额外给 `已自动补全项`
- 交互模式额外给 `方向决策摘要`

## 输出骨架

### 单片段

- 视频概述
- 主要约束
- 镜头级时间轴
- 镜头 Prompt
- 备选 Prompt
- 已自动补全项或方向决策摘要

### 多片段

- 视频概述
- 主要约束
- 分段时间轴
- 每段镜头级时间轴
- 镜头 Prompt
- 备选方案

## 严禁事项

- 不只写风格，不写时间顺序
- 不把主体运动和镜头运动混成一句空话
- 不用一堆导演名代替镜头、节拍和光影变量
- 不把维护说明写进运行期输出
